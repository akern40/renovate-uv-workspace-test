[project]
name = "renovate-uv-workspace-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["numpy"]

[tool.uv.workspace]
members = ["subpackage"]
